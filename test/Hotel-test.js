import chai from 'chai';
const expect = chai.expect;
import Hotel from '../src/Hotel.js';

describe('Should create a new instance of Hotel', () => {
  it('should return true for Hotel', () => {
    expect(true).to.equal(true);
  });
});